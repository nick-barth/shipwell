// Vendors
import * as React from 'react';
import { RouteProps } from 'react-router';

export class Layout extends React.Component<{} & RouteProps, {}> {
	public render () {
		return (
            <div> wow </div>
		);
	}
}
